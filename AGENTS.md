# AI 开发协作规范

> 项目：加拿大移民志  
> 状态：工作稿 v0.1  
> 更新日期：2026-09-04

本文件适用于参与本项目的 AI coding agent、内容辅助 agent 和自动化任务。它与产品设计、内容模型、编辑规范、来源规范、技术设计和里程碑文档一起使用。

## 1. 项目目标

本项目是一个面向中文读者的加拿大移民政策与现实信息网站，目标是帮助用户：

- 理解加拿大移民法律、政策、项目和数据；
- 区分已宣布、已生效、咨询中、暂停和已结束的政策；
- 找到适用于自身情况的当前项目指南；
- 回溯到可靠的官方来源。

网站不是移民中介门户，不提供个案法律意见、成功率预测或结果承诺。

## 2. 当前范围

当前 MVP 聚焦：

- 计划来加拿大留学、工作并长期发展的用户；
- 已在加拿大、等待永久居民或长期身份路径的用户；
- Express Entry；
- BC、Ontario 和 Québec 的重点项目；
- 8–10 篇新闻、2 篇政策解释、3 个核心项目指南和 3 个省级概览（BC、Ontario、Québec）。

首版不扩展到全部省份、全部移民 stream、自动资格评估、账户体系、邮件订阅、复杂数据查询或自动发布。

项目名称为“加拿大移民志”，副标题为“看懂加拿大移民政策与现实”。

## 3. AI 的职责边界

AI 可以：

- 发现和整理官方来源；
- 提出选题和内容结构；
- 生成中文初稿；
- 提取日期、费用、配额和适用人群等字段；
- 检查重复内容、内部链接、格式和 Schema；
- 编写和测试网站代码；
- 发现文档之间的矛盾并提出修改建议。

AI 不得：

- 在没有来源的情况下补全政策事实；
- 把政治表态、媒体报道或个案经验写成正式规则；
- 自动决定复杂法律问题；
- 自动发布新闻或修改已发布内容；
- 给具体用户作出资格判断、成功承诺或行动指令；
- 为了搜索流量使用夸张、制造焦虑或误导性的标题。

涉及资格、法律效果、政策状态、适用人群和行动建议的内容，必须由人类审核后才能发布。当前初版由用户负责最终审核。

## 4. 信息来源规范

涉及资格、程序、费用、配额、截止日期和法律效果时，优先使用：

1. 法律法规、正式指令和官方数据；
2. 政府部门的项目规则、实施说明和正式网页；
3. 官方新闻稿、FAQ、背景文件和讲话；
4. 可靠媒体、研究机构和专业分析。

社交媒体、顾问文章和申请人经验只能作为线索，不能单独作为关键事实的最终依据。

每次内容工作都应记录来源机构、来源类型、URL、来源语言、发布日期、访问日期、来源是否仍然有效，以及来源支持文章中的哪些事实。

如果官方来源之间存在冲突，AI 必须指出冲突，不得自行合并成确定结论。

## 5. 内容写作规范

每篇新闻至少回答：

1. 发生了什么；
2. 政策状态是什么；
3. 何时发布、何时生效或截止；
4. 影响哪些人；
5. 与旧规则有什么不同；
6. 哪些问题仍未确定；
7. 官方依据在哪里。

必须区分事实、解释、判断和未知。没有官方日期时不得编造或推测生效日期。首次出现的专业术语保留英文或法文官方名称。

使用以下字段区分内容状态：

- publicationStatus：文章是否可以公开；
- policyStatus：政策处于什么状态；
- review.status：是否通过人工审核。

publicationStatus 为 published 且 review.status 为 approved 之前，内容不得进入生产页面、RSS 或 sitemap。

## 6. 技术开发规范

- 优先遵循 technical-design.md 中的架构；
- 正式内容使用 Markdown，结构化数据使用 JSON/CSV；
- 内容必须通过 Schema 和受控词表校验；
- 不绕过构建检查、类型检查、lint 或测试；
- 不把搜索、图表或部署平台的具体实现泄漏到内容模型；
- 默认使用服务端渲染或静态生成，谨慎增加客户端 JavaScript；
- 默认安全转义 Markdown，不引入不受信任的脚本或组件；
- 官方来源 URL 只允许 HTTPS，并通过域名白名单校验；
- 修改 slug 时必须同步维护重定向；
- 涉及内容状态、来源、版本和发布流程的改动，必须同步检查相关文档。

## 7. 工作方式

开始任务前：

- 阅读与任务相关的设计文档；
- 检查当前 Git 状态；
- 明确任务范围和不包含的内容；
- 识别是否涉及政策事实、外部资料或隐私风险。

执行任务时：

- 优先做小范围、可验证的修改；
- 保留用户已有的未提交修改；
- 不修改任务范围外的文件；
- 不使用破坏性 Git 命令；
- 遇到文档冲突时先报告，并采用较严格的安全规则；
- 不能确认的事实标记为待确认，不猜测。

完成任务前：

- 运行与改动相匹配的检查；
- 检查内部引用、状态字段和日期逻辑；
- 总结修改的文件、验证结果和遗留问题；
- 只有用户明确要求时才创建 commit。

## 8. 内容工作流

初版内容按照以下流程进行：

来源发现 → 选题整理 → AI 初稿 → 来源与字段核验 → 用户人工审核 → 发布

AI 输出内容时，应把事实摘要、官方来源、仍待核验的问题、中文文章初稿，以及对项目指南或旧文章的影响分开提供。

初版由用户完成最终审核。未来可以将候选新闻、来源版本、审核记录和发布状态发展成更成熟的工作流，但自动化内容始终只能进入草稿或独立分支。

## 9. Git 与提交规范

提交信息使用简短、明确的英文 conventional commit 风格，例如：

- docs: refine content model
- feat: add article detail page
- fix: validate published content
- test: add content schema coverage

一个 commit 尽量只包含一个逻辑变化。提交前检查是否包含无关文件、草稿、密钥或本地配置，并确认必要检查已经通过。

## 10. 完成定义

一项开发任务只有在以下条件都满足时才算完成：

- 实现符合当前 MVP 范围；
- 内容和代码通过相关校验；
- 关键路径有测试或手动验证记录；
- 没有绕过人工审核边界；
- 相关设计文档已同步；
- 遗留问题和后续工作已明确记录。

里程碑级任务还必须满足 milestones.md 中对应的完成定义。

## 11. 决策与变更

以下变化必须先更新设计文档或记录 ADR：

- 修改 MVP 范围；
- 引入数据库、账户或个性化功能；
- 改变内容状态和人工审核门槛；
- 引入自动发布；
- 更换核心技术栈；
- 收集新的用户数据；
- 将 Québec、Ontario 或 BC 的项目模型合并成统一规则。

当本文件与用户最新明确决定冲突时，以用户决定为准，并同步更新本文件。
