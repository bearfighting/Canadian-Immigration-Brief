# 加拿大移民中文信息网站：技术设计

> 状态：工作稿 v0.2  
> 更新日期：2026-09-04

## 1. 技术目标

- 以静态内容为主，低成本、快速、可维护；
- 正式内容通过 Git 保存与审核；
- 首版不依赖数据库；
- 自动化只能生成草稿，不能绕过人工审核；
- 数据结构为未来数据库迁移保留清晰边界。

## 2. 推荐技术栈

- Next.js + TypeScript；
- Tailwind CSS；
- shadcn/ui 作为基础组件，不直接决定视觉风格；
- 普通 Markdown + YAML front matter；
- Zod 校验内容与数据；
- 构建时静态搜索索引；
- Vercel 或同类静态/混合部署；
- GitHub Actions 运行检查。

首版优先普通 Markdown，而不是允许任意 React 表达式的 MDX。确有交互图表需求时，对少量可信页面开放 MDX。具体依赖版本和库选择在实现前锁定，并写入 package.json 与 ADR。

## 3. 建议代码结构

```text
app/
├── (site)/
│   ├── page.tsx
│   ├── news/
│   ├── policies/
│   ├── programs/
│   ├── provinces/
│   ├── data/
│   └── search/
├── rss.xml/
└── sitemap.ts

components/
├── content/
├── policy/
├── navigation/
└── charts/

content/
data/
lib/
├── content/
├── search/
├── seo/
└── validation/
```

## 4. 内容构建流程

```text
Markdown/JSON
  -> 解析 front matter
  -> Zod 校验
  -> 解析 Markdown AST
  -> 建立内容关系
  -> 生成静态页面
  -> 生成搜索索引、RSS、sitemap 和 JSON-LD
```

构建必须在以下情况失败：非法标签、重复 ID/slug、发布内容缺少官方来源、未批准内容被标记为发布、无法解析的项目引用、失效的版本关系或不合理日期。

## 5. 渲染与安全

- Markdown HTML 默认转义；
- 禁止不受信任的内联脚本和任意组件；
- 外部链接增加合理的安全属性；
- 官方来源 URL 仅允许 HTTPS，并通过维护中的官方域名白名单检查；
- 自动生成文件只进入草稿目录或独立分支；
- 生产构建排除 `publicationStatus: draft` 和 `review.status != approved`。

## 6. 搜索

首版使用构建时静态索引，不需要数据库全文搜索。索引正文、标题、description、官方名称、缩写、中文别名、项目、省份、受众和状态。

搜索结果支持：

- 内容类型过滤；
- 省份和主题过滤；
- 当前指南与历史新闻区分；
- 常用中文简称和错别名映射；
- 无结果关键词统计（在符合隐私策略的前提下）。

首版先选择一种构建时搜索实现并封装接口；搜索实现不应渗透到内容模型和页面组件中。

## 7. SEO

- 稳定、无日期依赖的公开 slug；
- canonical URL；
- sitemap 与新闻/栏目 RSS；
- `NewsArticle`、`Article` 和 `BreadcrumbList` JSON-LD；
- `datePublished`、`dateModified` 和作者信息；
- 当前指南与历史新闻使用不同的结构化数据类型和视觉标识；
- Open Graph 和分享图；
- 标签页达到最低内容量后才允许索引；
- 搜索、组合筛选、草稿和薄内容页设置 `noindex`。

## 8. 性能

- 页面以静态生成为主；
- 限制客户端 JavaScript；
- 不为新闻强制加载大图；
- 字体子集化并合理回退；
- 图表按需加载；
- 目标是主要内容无需等待客户端水合即可阅读。

## 9. 自动化编辑流程

```text
定时来源扫描
  -> 候选新闻与去重
  -> 字段提取和中文初稿
  -> draft 文件或 Pull Request
  -> 人工核验与修改
  -> review.status=approved
  -> 合并并发布
```

早期可以手工将定期选题简报转成文章。待来源和流程稳定后，再开发采集器及网页差异检测。

## 10. 测试

### 单元测试

- Schema 与受控词表；
- 日期和状态逻辑；
- 内容关系解析；
- 术语别名；
- SEO metadata。

### 构建测试

- 所有已发布页面可生成；
- 内部链接无断链；
- sitemap、RSS 和 JSON-LD 合法；
- 草稿不可公开访问。
- 生产构建不包含未批准内容和测试数据。

### UI 测试

- 首页、新闻页、项目页和搜索页关键视口；
- 状态标签不仅依靠颜色；
- 手机端表格可用；
- 键盘导航和焦点状态。

## 11. 分析与隐私

首版只收集支持产品改进的最少数据：页面访问、入口、搜索词、无结果搜索、项目页回访和官方来源点击。避免跨站广告追踪；隐私政策应说明收集内容、法律依据、保留期限、退出方式和第三方服务。

## 12. 备份和内容完整性

- Git 保存文章、数据、术语和重定向；
- 定期验证外部链接；
- 图片和图表资产使用稳定路径；
- slug 变化必须增加重定向；
- 数据文件带 `schemaVersion` 和 `updatedAt`；
- 重要官方页面未来可保存摘要、哈希和访问日期，避免复制完整版权内容。
- 发布前应有 staging 预览环境；生产发布失败时可以回滚到上一个 Git 版本；
- CI 至少运行 Schema 校验、链接检查、构建检查和关键页面 smoke test。

## 13. 数据库引入边界

数据库优先用于：候选新闻、网页快照、抓取状态、订阅偏好、通知历史、账户和复杂数据筛选。正式出版文章可以继续保留在 Markdown。

推荐演进：

1. 静态 Markdown + JSON；
2. Markdown 出版 + Postgres 编辑工作流；
3. 在真实需求出现后加入账户、个性化与专业数据服务。

## 14. 上线检查

- [ ] 内容 Schema 全部通过；
- [ ] 核心页面移动端和桌面端验收；
- [ ] 404、重定向和失效内容提示可用；
- [ ] sitemap、RSS、robots 和 canonical 正确；
- [ ] 编辑标准、免责声明和隐私政策上线；
- [ ] 监控构建失败与运行错误；
- [ ] 准备回滚方式；
- [ ] 至少完成一轮真实新闻发布演练。

## 15. 待决事项

- Next.js 具体版本与内容解析库；
- 静态搜索实现选择；
- 分析工具；
- 邮件订阅服务；
- 图片存储策略；
- 是否建立独立 staging 域名；
- 自动化初稿采用文件、Issue 还是 Pull Request。
